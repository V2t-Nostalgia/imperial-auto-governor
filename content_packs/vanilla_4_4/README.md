# Stellaris Vanilla 4.4 Content Pack

当前 pack 保存 v0.5.8 已验证的 Stellaris 4.4 原版建设能力映射。`pack.toml` 声明版本和平台依赖，`mappings/capabilities.json` 被经济规则引擎读取，`prompts/stellaris_4_4_sources.md` 记录定义来源与使用边界。

游戏大版本更新后必须重新验证 manifest 版本范围、对象 ID、槽位语义和执行协议，不能只修改版本字符串。
