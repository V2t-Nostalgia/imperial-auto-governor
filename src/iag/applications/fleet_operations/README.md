# 舰队行动 Application

该 Application 读取最新同步存档中的玩家舰队、军力、移动、交战与 MIA 状态，
并把玩家逐舰队授权作为本地强制边界。新发现的舰队默认禁止模型调用。

`d32c` 移动命令已经完成真实会话闭环，但仍依赖 Windows 会话代理，必须在加入
房间前启动并保持到退出房间。`6b33` 攻击记录目前只有房主权威样本，缺少非房主
请求与回包的成对验证；前端保留攻击授权开关，但执行器会在补齐该样本前拒绝发包。
