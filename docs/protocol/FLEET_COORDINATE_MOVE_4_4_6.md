# Stellaris 4.4.6 星系内舰队坐标移动记录

本文件记录用于回归测试的结构结论，不包含会话地址、端口、完整抓包或用户路径。

## 已确认字段

`4f2c01000300` 记录固定为 144 字节。主要目标字段为：

| 偏移 | 含义 |
|---:|---|
| `70..79` | `502c` 标签和来源舰队对象 |
| `80..85` | `6b0001000300` 坐标容器 |
| `86..99` | X 标签和有符号 64 位小端 X |
| `100..113` | Y 标签和有符号 64 位小端 Y |
| `114..123` | `a72c` 标签和当前星系 origin |
| `126..132` | `6340` 标志，当前成对样本值为 0 |
| `133..139` | `de35` 标志，当前成对样本值为 0 |
| `140..143` | `04000400` 尾部 |

X/Y 的协议整数等于存档十进制坐标乘以 `100000`。构造器保留两个未知标志为已验证的 0，只允许改写 actor、origin、serial、舰队、X、Y 和当前星系 origin；构造完成后会重新解析全部目标字段。

## 验证等级

- 四方向非房主请求与房主回传：已有成对样本。
- 离线精确 fixture 与目标关联：已通过。
- 存档当前位置、行星坐标和逐舰队权限校验：已通过。
- 新自主执行链实机闭环：待下一轮测试。
